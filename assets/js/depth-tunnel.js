/* ============================================
   3D DEPTH TUNNEL ENGINE
   (DISABLED)
   --------------------------------------------
   The original Three.js "room" / depth-tunnel scene is intentionally disabled
   to keep animations stable and lightweight across devices.
   
   If you want to re-enable later, restore the previous implementation.
   ============================================ */

(() => {
  'use strict';

  // Respect reduced motion and avoid running heavy WebGL.
  // No-op module.
  return;
})();
